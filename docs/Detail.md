# Detail: docs

## 这一层在做什么
docs 是文档素材层，专门放支撑说明文档展示的静态资源。

## 这一层直接包含什么
### 直接子目录
- 本层没有直接子目录。

### 直接文件
- bek-feedback-group.png（551 B）

## 这一层如何实现自己的职责
当前只有反馈群二维码图片，它被 README 直接引用，因此这里更像“文档配图附件层”而不是 API 文档源码层。

## 这一层与其他层级的关系
它向上服务根目录 Markdown 文档，向下没有再细分的功能结构。

## 阅读这一层时要注意什么
内容不多，但用户在 README 中很容易第一时间看见它。
