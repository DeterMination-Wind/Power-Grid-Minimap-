# Detail: src\main\java

## 这一层在做什么
src/main/java 是 Java 源码入口层，所有运行时逻辑都从这里编译出来。

## 这一层直接包含什么
### 直接子目录
- powergridminimap

### 直接文件
- 本层没有直接文件，职责主要体现在子目录与其生成关系上。

## 这一层如何实现自己的职责
当前只有一个 powergridminimap 包，说明项目没有横向包拆分，而是把复杂度集中在单包内部。

## 这一层与其他层级的关系
它向下进入包层源码文件，向上直接被 compileJava 消费，与 resources 构成“逻辑 + 文本资源”的并行输入。

## 阅读这一层时要注意什么
由于包层级简单，理解项目时真正要关注的是文件内边界和内部类切分。
